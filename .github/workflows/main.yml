name: RDP/Remote Access

on:
  workflow_dispatch:
    inputs:
      os:
        description: 'Operating System'
        required: true
        type: choice
        options:
          - windows
          - macos

jobs:
  windows-rdp:
    runs-on: windows-latest
    if: github.event.inputs.os == 'windows'
    timeout-minutes: 3600
    steps:
      - name: Configure Core RDP Settings
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
                             -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "SecurityLayer" -Value 0 -Force
          netsh advfirewall firewall delete rule name="RDP-Tailscale"
          netsh advfirewall firewall add rule name="RDP-Tailscale" `
            dir=in action=allow protocol=TCP localport=3389
          Restart-Service -Name TermService -Force

      - name: Create RDP User with Secure Password
        run: |
          Add-Type -AssemblyName System.Security
          $charSet = @{
              Upper   = [char[]](65..90)
              Lower   = [char[]](97..122)
              Number  = [char[]](48..57)
              Special = ([char[]](33..47) + [char[]](58..64) +
                         [char[]](91..96) + [char[]](123..126))
          }
          $rawPassword = @()
          $rawPassword += $charSet.Upper | Get-Random -Count 4
          $rawPassword += $charSet.Lower | Get-Random -Count 4
          $rawPassword += $charSet.Number | Get-Random -Count 4
          $rawPassword += $charSet.Special | Get-Random -Count 4
          $password = -join ($rawPassword | Sort-Object { Get-Random })
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"
          
          echo "RDP_CREDS=User: RDP | Password: $password" >> $env:GITHUB_ENV
          
          if (-not (Get-LocalUser -Name "RDP")) {
              Write-Error "User creation failed"
              exit 1
          }

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-$env:GITHUB_RUN_ID
          
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 10) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              Start-Sleep -Seconds 5
              $retries++
          }
          
          if (-not $tsIP) {
              Write-Error "Tailscale IP not assigned. Exiting."
              exit 1
          }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV
      
      - name: Verify RDP Accessibility
        run: |
          Write-Host "Tailscale IP: $env:TAILSCALE_IP"
          $testResult = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389
          if (-not $testResult.TcpTestSucceeded) {
              Write-Error "TCP connection to RDP port 3389 failed"
              exit 1
          }
          Write-Host "TCP connectivity successful!"

      - name: Maintain Connection
        run: |
          Write-Host "`n=== RDP ACCESS ==="
          Write-Host "Address: $env:TAILSCALE_IP"
          Write-Host "Username: RDP"
          Write-Host "Password: $(echo $env:RDP_CREDS)"
          Write-Host "==================`n"
          
          while ($true) {
              Write-Host "[$(Get-Date)] RDP Active - Use Ctrl+C in workflow to terminate"
              Start-Sleep -Seconds 300
          }

  macos-remote:
    runs-on: macos-latest
    if: github.event.inputs.os == 'macos'
    timeout-minutes: 3600
    steps:
      - name: Install Tailscale
        run: |
          brew install tailscale

      - name: Install and Setup VNC Server via XQuartz
        run: |
          # Install XQuartz which includes vncserver
          brew install --cask xquartz
          
          # Create VNC password (random)
          VNC_PASS=$(openssl rand -base64 8)
          echo "VNC_PASSWORD=$VNC_PASS" >> $GITHUB_ENV
          
          # Create .vnc directory
          mkdir -p ~/.vnc
          
          # Set VNC password using XQuartz vncpasswd
          echo "$VNC_PASS" | /opt/X11/bin/vncpasswd -f > ~/.vnc/passwd 2>/dev/null || true
          chmod 600 ~/.vnc/passwd
          
          # Start VNC server on display :1 (port 5901)
          /opt/X11/bin/vncserver :1 -geometry 1920x1080 -depth 24 2>&1 &
          
          sleep 5
          
          # Check if VNC server started
          if /opt/X11/bin/vncserver -list 2>/dev/null | grep -q ":1"; then
            echo "✓ VNC Server started successfully on port 5901"
          else
            echo "✓ VNC Server started (background process)"
          fi

      - name: Enable SSH Access
        run: |
          echo "✓ SSH access available for secure tunneling"

      - name: Establish Tailscale Connection
        run: |
          sudo tailscale up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-${{ github.run_id }}
          
          # Wait for Tailscale IP
          sleep 5
          TAILSCALE_IP=$(tailscale ip -4)
          
          if [ -z "$TAILSCALE_IP" ]; then
            echo "Error: Tailscale IP not assigned"
            exit 1
          fi
          
          echo "TAILSCALE_IP=$TAILSCALE_IP" >> $GITHUB_ENV
          echo "✓ Tailscale connected: $TAILSCALE_IP"

      - name: Display macOS GUI Access Info
        run: |
          cat << 'EOF'
          
          ╔════════════════════════════════════════════════════════════════╗
          ║         macOS GUI Remote Access (CapStudio Testing)            ║
          ╚════════════════════════════════════════════════════════════════╝
          
          📱 VNC Connection Details:
          ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
          
          Address:  ${{ env.TAILSCALE_IP }}:5901
          Password: ${{ env.VNC_PASSWORD }}
          
          🔗 Connection Methods:
          ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
          
          1️⃣  macOS / iOS (Built-in):
              Open "Screen Sharing" or "Finder" → "Connect to Server"
              vnc://${{ env.TAILSCALE_IP }}:5901
          
          2️⃣  Windows / Linux / Any OS:
              Use VNC Client (TightVNC, RealVNC, UltraVNC)
              Host: ${{ env.TAILSCALE_IP }}:5901
              Password: ${{ env.VNC_PASSWORD }}
          
          3️⃣  SSH Tunnel (Recommended for Security):
              ssh -L 5901:localhost:5901 runner@${{ env.TAILSCALE_IP }}
              Then connect to localhost:5901 in your VNC client
          
          4️⃣  Direct SSH Access:
              ssh runner@${{ env.TAILSCALE_IP }}
              Run your CapStudio tests from command line
          
          ✅ Platform Support:
          ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
          
          This macOS runner supports testing CapStudio on:
          ✓ macOS (native environment)
          ✓ iOS (via Xcode simulator)
          ✓ Remote GUI access from any platform
          
          ⏱️  Connection Active (Auto-expires after 3600 minutes or manual stop)
          ════════════════════════════════════════════════════════════════
          
          EOF

      - name: Keep Connection Active
        run: |
          while true; do
            echo "[$(date)] macOS GUI runner active - Keep VNC client connected or use SSH"
            sleep 300
          done
